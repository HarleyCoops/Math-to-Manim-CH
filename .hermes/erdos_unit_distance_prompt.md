把 Erdős unit distance conjecture 的完整故事做成一段清晰、干净、以 lattice 为中心的史诗级动画，视觉灵感来自参考图 `C:\Users\chris\Downloads\erdos.png`（`/mnt/c/Users/chris/Downloads/erdos.png`）。该图展示一个密集 unit-distance graph，标题为“Unit-distance graph on a + bi + cφ + diρ, a,b,c,d∈{-2,-1,0,1,2}”，包含 complex plane 中橙色 lattice-like points、蓝色 unit-distance edges、坐标轴 Re(z)、Im(z)，以及一个 circular/hexagonal cloud，内部隐藏大量 unit edges。使用它作为视觉灵感，而不是平面复制。

创建一个 off-white bone-colored 3D Manim space（`#F4EBDD` 或类似颜色），使用丰富但干净的颜色：deep ink labels、sapphire/blue unit edges、amber/gold points、violet algebraic layers，以及半透明 glass-like connection sheets/edges。风格应像严谨的数学博物馆：干净地解释 lattice，不做拥挤 slide。关键视觉实验是 glass effect：unit-distance connections 应呈现为半透明蓝/紫 glass rods 或 panes，随着相机穿越 graph 变得更透明并消失，从而揭示 lattice structure，而不是遮挡它。

叙事弧线：从平面中的稀疏点集 P 和问题 `u(n)=max_{|P|=n}|{{p,q}: |p-q|=1}|` 开始。展示一对点，测量 `|p-q|=1`，然后把每个点变成 vertex，把每条 unit segment 变成 graph edge。从最简单的 line construction `n-1` 开始，再进入 square/triangular lattice intuition，解释为什么 lattice 可以隐藏许多重复 unit distances。从普通 integer lattice points `a+bi` 过渡到图中的 algebraic lattice expression `a+bi+cφ+diρ`，其中 `a,b,c,d∈{-2,-1,0,1,2}`。大幅渲染该公式，再 zoom into 每个 symbol：`a+bi` 变成平面 Gaussian integer grid；`cφ` 和 `diρ` 变成额外 algebraic offsets/layers，抬升到 3D、旋转，并投影回密集 planar cloud。Unit circles 应围绕选中点 pulse；交点生成许多橙色 points 和蓝色 edges。

清楚展示的数学节拍：定义 unit-distance graph；展示 `u(n)`；解释 conjectural target `u(n)≤n^{1+o(1)}`；展示大量 examples 与 incidence-geometry upper bounds（如 `O(n^{4/3})`）之间的经典张力；然后展示 algebraic number theory constructions 如何通过把许多 unit edges 塞进结构化 finite sets 来挑战简单直觉。当 3D geometry 在背后移动时，使用固定在 frame 中的可读 formula cards。包含 reference-image formula 和 coefficient range `{-2,-1,0,1,2}`；把 coefficients 映射到 small sliders 或 glowing integer ticks，然后让所有 combinations bloom 成 point cloud。保持解释诚实：这是 unit-distance problem 和 algebraic/lattice constructions 的视觉解释，不是最终定理的证明。

相机编舞：从 bone 3D plane 的低空 flyover 开始；下降到干净 grid；构建 amber points；用 sapphire 画 unit edges；抬升到带 violet transparent planes 的 algebraic layers；穿过密集 glass forest of unit edges，近处 connections 像折射 glass 一样淡出；最后 zoom out 到接近参考图的完整 graph，包含 axes Re(z)、Im(z)、orange points、blue edges，以及最终 takeaway：“Unit distances are geometry; many unit distances come from hidden algebraic structure.” 让动画 motion-rich 但可读，在每个 formula reveal 后留 breathing pauses，并保持干净 final tableau。
